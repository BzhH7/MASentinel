# MASentinel 项目技术路线与方案汇报

## 1. 项目定位

MASentinel 是一个面向 AutoGen 多智能体系统的自动化测试框架。项目目标不是简单运行一组人工测试脚本，而是在无人干预条件下完成：

1. 自动生成测试用例；
2. 自动执行测试并判定通过/失败；
3. 自动诊断故障并生成报告。

这一技术路线直接对应命题对“测试用例生成、测试运行、故障诊断”三阶段的要求，同时强调多智能体系统的语义覆盖率，而不是只依赖传统代码覆盖率。

## 2. 总体技术路线

MASentinel 的自动化评测流程如下：

```text
评测前配置
  ↓
静态解析源码与文档
  ↓
构建 Agent-Tool-Message-State 语义画像
  ↓
DeepSeek V4 pro 驱动测试智能体生成测例
  ↓
测例校验与冻结
  ↓
subprocess 隔离执行被测 AutoGen 系统
  ↓
采集 stdout/stderr、MAS_TRACE、tool call、exception、timeout 等 trace
  ↓
规则 oracle 自动判定 pass/fail
  ↓
故障分类、去重、误报审计
  ↓
生成 coverage、fault report、HTML dashboard 与最终报告
```

核心思想是：先把被测系统抽象成语义图，再围绕 agent、tool、message edge、requirement、state、fault mode 设计测试，而不是只检查代码行是否执行。

## 3. 三阶段方案设计

### Step 1：自动生成测例

输入包括系统源码、README/需求文档、配置文件、静态分析出的 agents/tools/message edges，以及文档需求。

生成方式由两部分组成：

- 测试智能体：`RequirementAnalystAgent`、`SystemModelingAgent`、`TestDesignerAgent` 使用 DeepSeek V4 pro 抽取需求、审查语义图、生成候选测例；
- 确定性生成器：补齐 requirement、coverage-guided、property-boundary、fuzz、tool-failure、metamorphic、regression 等关键测例类型，保证 API 不可用时流程仍可运行。

测例生成后会自动校验并冻结，核心产物包括：

```text
testcases.generated.json
testcases.validated.json
testcases.frozen.sha256
step1_generation_summary.json
agent_trace.jsonl
model_usage.json
```

这些文件用于证明测例确实由系统自动生成，且生成后没有人工筛选或修改。

### Step 2：自动执行测试并判定

Runner 会加载冻结后的测例，通过 subprocess 隔离运行被测系统，并自动注入输入。运行时设置：

```text
MAS_TEST_CASE_ID
MAS_TRACE_PATH
MAS_MAX_TURNS
MAS_NO_HUMAN=1
MAS_FAULT_INJECTION
```

为避免把“被测系统没跑起来”误当成最终测试能力，Runner 支持按测试用例渲染 `stdin_template` 和 `run.env`，并通过 `PYTHONPATH` 注入 MASentinel 的 `runtime_patches/sitecustomize.py`。这类适配发生在目标进程启动时，不需要修改被测系统源码。

当前运行适配能力包括：

- OpenAI-compatible API 替换：将题目给定的 DeepSeek Flash 写入 `OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_API_BASE`、`OAI_CONFIG_LIST` 和 `MAS_MODEL_NAME`，被测系统中原本写死的 OpenAI model/config 会在运行时被改写。
- AutoGen 兼容：关闭 Docker 执行、设置 `human_input_mode=NEVER`、移除新版 AutoGen 不再接受的 `request_timeout` 参数、为每个 case 分配独立工作目录。
- Assistants API 降级：对强绑定 OpenAI Assistants 的 `GPTAssistantAgent`，运行时替换为普通 `AssistantAgent`，继续使用 DeepSeek Flash 完成多智能体协作。
- 依赖/API 兼容：补齐新版 LangChain 包拆分的旧导入路径，外部 HTTP 工具可在评测中 mock，yfinance 限流或网络不可用时可提供确定性财务数据。

系统自动采集每条 case 的 trace，并由规则 oracle 判定结果。若目标系统在评测开始后请求人工输入，会被记录为 `HUMAN_INPUT_REQUESTED`。

核心产物包括：

```text
runs/traces/*.json
runs/run_summary.json
oracle_results.json
rule_results.json
```

### Step 3：自动诊断并生成故障报告

诊断阶段先由规则 oracle 给出确定性 failure code，再由 `FaultDiagnoserAgent` 和 `FalsePositiveAuditorAgent` 对故障层级、根因、修复建议和误报风险进行分析。

故障检测边界限定在应用层软件代码和 AutoGen 框架/配置/工具集成层。也就是说，只有能通过修改被测系统代码、提示模板、工具 schema/function_map、终止条件、AutoGen 参数或运行适配来缓解的问题，才进入 `faults.json`。纯粹的大模型回答风格、知识不足、模型参数表现、模型服务鉴权/限流/超时，以及测试框架输入预算问题，会进入 `non_target_issues.json` 或误报审计，不作为目标系统故障计数。

当前重点检测的故障包括：

- 运行时异常：`RUNTIME_EXCEPTION`
- 超时与非终止：`TIMEOUT`、`NON_TERMINATION`
- 消息路由异常：`MISSING_AGENT`、`MISSING_MESSAGE_EDGE`
- 工具调用异常：`MISSING_TOOL_CALL`、`TOOL_SCHEMA_MISMATCH`、`TOOL_HALLUCINATION`
- 输出契约异常：`OUTPUT_EMPTY`、`OUTPUT_SCHEMA_VIOLATION`
- 自动化违规：`HUMAN_INPUT_REQUESTED`
- 变形测试关系破坏：`METAMORPHIC_RELATION_VIOLATION`

核心产物包括：

```text
faults.raw.json
faults.json
non_target_issues.json
false_positive_audit.json
coverage.json
fault_report.md
report.md
report.html
dashboard.html
patch_suggestions.md
flaky_report.json
trace_graph.json
trace_graph.dot
```

## 4. 多智能体测试系统设计

MASentinel 自身也采用测试智能体协作结构，DeepSeek V4 pro 主要用于测试方案侧推理，而不是替换被测系统内部模型。

| 测试智能体 | 职责 | 主要产物 |
| --- | --- | --- |
| `RequirementAnalystAgent` | 从文档和代码摘要中抽取可测试需求 | requirements/profile 更新 |
| `SystemModelingAgent` | 审查 agent/tool/message graph | semantic graph review |
| `TestDesignerAgent` | 生成候选测试用例 | testcases.generated.json |
| `CoverageStrategistAgent` | 分析覆盖缺口并触发二轮补测 | coverage_strategy.json |
| `ExecutionMonitorAgent` | 汇总异常运行与 suspicious cases | execution_monitor.json |
| `FaultDiagnoserAgent` | 分析失败 trace 的故障层级、根因、修复建议 | faults.json |
| `FalsePositiveAuditorAgent` | 区分 confirmed/suspected/likely false positive | false_positive_audit.json |
| `ReportWriterAgent` | 生成报告叙述与效果分析 | report.md/report.html |

所有 agent 调用都会记录到 `agent_trace.jsonl` 和 `model_usage.json`。当 DeepSeek V4 pro 未配置或调用失败时，系统使用确定性 fallback，并把 fallback 次数写入 usage 文件，保证评测流程不中断且可审计。

## 5. 模型分工

命题中的模型分工被拆成两组配置：

- 测试方案模型：DeepSeek V4 pro，用于 MASentinel 内部测试智能体；
- 被测系统模型：DeepSeek V4 flash API 或本地 `qwen2.5-coder:7b`，传给目标 AutoGen 系统。

模型在这里是被测系统运行依赖和测试智能体推理工具，不是故障归因对象。模型服务异常会被单独记录，模型回答质量只有在可归因于应用提示、输出契约、工具调用或 AutoGen 编排代码时，才会转化为应用/框架层故障。

推荐环境变量：

```bash
export INF_API_KEY_PRO="..."
export INF_API_KEY_FLASH="..."
```

配置中对应：

```yaml
model:
  testing_model: ds-v4-pro
  testing_api_key_env: INF_API_KEY_PRO
  target_model: ds-v4-flash
  target_api_key_env: INF_API_KEY_FLASH
```

## 6. 语义覆盖率设计

MASentinel 采用 MASCov 作为多智能体系统综合语义覆盖率：

```text
MASCov =
0.18 * AgentCoverage
+ 0.18 * ToolCoverage
+ 0.16 * MessageEdgeCoverage
+ 0.16 * RequirementCoverage
+ 0.16 * StateCoverage
+ 0.16 * FaultModeCoverage
```

各指标含义：

- AgentCoverage：执行 trace 中访问到的 agent 比例；
- ToolCoverage：实际调用过的工具比例；
- MessageEdgeCoverage：观测到的 agent 间消息边比例；
- RequirementCoverage：被测例覆盖的需求比例；
- StateCoverage：空输入、格式错误、工具失败、多轮记忆、终止、异常等状态覆盖；
- FaultModeCoverage：工具 schema、工具缺失、错误路由、非终止、人机输入违规等故障模式覆盖。

覆盖率不是只用于展示，agentic 模式会在覆盖不足时自动触发一次 coverage-guided 二轮补测，并输出 `second_round_summary.json`。

## 7. 无人干预证据链

为了满足“开始生成测例到最终故障报告不得人工干预”的要求，MASentinel 在运行开始时生成：

```text
run_manifest.json
```

其中包含：

- run id 和开始时间；
- config hash；
- code snapshot hash；
- doc hash；
- entry command；
- Python executable；
- `human_intervention_allowed: false`。

运行期间：

- 测例生成后写入 `testcases.frozen.sha256`；
- 执行阶段设置 `MAS_NO_HUMAN=1`；
- 发现人工输入请求时判为 `HUMAN_INPUT_REQUESTED`；
- 所有 agent 调用、fallback、模型 token 估计写入 `model_usage.json`。

这条证据链用于回答“测例是不是人工写的”“失败是不是人工判断的”“报告是不是人工整理的”等答辩问题。

## 8. 当前实现状态

当前版本已经完成：

- P0：三阶段主流程、自动测例生成/校验/冻结、自动执行、自动 oracle、自动故障报告、无人干预 manifest；
- P1：内部测试智能体、DeepSeek V4 pro 使用记录、误报审计、覆盖驱动二轮补测、工具故障注入、故障去重；
- P2：HTML dashboard、trace graph、patch suggestions、flaky report、回归池。
- 针对三个真实被测系统的适配增强：支持 `run.message_template` 覆盖硬编码 `initiate_chat(message=...)`，支持 `{stock_symbol}` 命令模板驱动金融分析系统按不同 case 运行，runtime patch 会捕获 `initiate_chat/send/receive` 并输出 `MAS_TRACE`。
- 针对误报控制的增强：当 case 没有观测到有效 agent workflow 时，oracle 将其归类为 `TARGET_WORKFLOW_NOT_OBSERVED` 等 test-harness 问题，排除出应用层/AutoGen 框架故障。

本地 toy 验证结果：

```text
validated cases: 12
executed cases: 16
second-round extra cases: 4
case types: coverage_guided, fuzz_negative, fuzz_tool_failure, metamorphic, property_boundary, regression, requirement_positive
oracle_results: 16
human_intervention_allowed: false
```

由于本地未配置 DeepSeek V4 pro API key，测试智能体会 fallback 到确定性逻辑；服务器配置 `INF_API_KEY_PRO` 后，会真实调用 DeepSeek V4 pro 并在 `model_usage.json` 中体现。

## 9. 对 GPT 评价意见的回应

`gpt评价.md` 中指出的高分关键点主要有七项：

1. 是否有 `testcases.generated.json`；
2. 是否有 `testcases.validated.json`；
3. 是否有 `testcases.frozen.sha256`；
4. 是否有 `traces/*.json`；
5. 是否有 `oracle_results.json`；
6. 是否有 `faults.json` 与 `false_positive_audit.json`；
7. 是否有 `model_usage.json` 证明 DeepSeek V4 pro 参与测试方案。

当前实现已经覆盖这七项，并且增加了 `run_manifest.json`、`dashboard.html`、`trace_graph.json`、`patch_suggestions.md`、`flaky_report.json` 等辅助证据。

下一步最重要的是在服务器上用 `deepseek-v4-pro/deepseek-v4-flash` 完成三个目标系统的真实重跑，补齐三套 `outputs/system*/` 结果，并在最终报告中挑选典型 confirmed fault 与 likely false positive 做案例分析。

## 10. 汇报结论

MASentinel 的最终收敛表述可以概括为：

> MASentinel 是一个由 DeepSeek V4 pro 驱动的 AutoGen 多智能体系统自动化测试框架。它在无人干预条件下完成测试用例生成、测试运行和故障诊断，并通过 Agent/Tool/Message/State/Fault 语义覆盖率衡量测试充分性。

该方案的优势是：

- 与命题三阶段要求直接对齐；
- DeepSeek V4 pro 的使用位置清晰、可追踪；
- 自动化证据链完整；
- 覆盖率指标贴合多智能体系统；
- 故障报告包含根因、证据、修复建议与误报审计；
- 可在 API 不可用时 fallback，保证工程流程可复现。
