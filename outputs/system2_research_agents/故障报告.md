# MASentinel 故障报告：system2_research_agents

> 本报告由 MASentinel 自动化测试系统基于该被测系统的 `coverage.json`、`faults.json`、`false_positive_audit.json`、`non_target_issues.json` 和运行 trace 自动生成。

## 测试结果概览

| Cases | 进程通过 | 进程失败 | Oracle 通过 | Oracle 失败 | 确认主根因 | 派生症状 | 疑似误报 | 非目标排除 |
|-------|----------|----------|-------------|-------------|------------|----------|----------|------------|
| 32 | 25 | 7 | 3 | 29 | 5 | 1 | 3 | 13 |

## 覆盖率

| AgentCov | ToolCov | EdgeCov | ReqIntent | ReqVerified | ContractCov | EffWorkflow | TraceComplete | EvidenceRate | StateCov | FaultCov | MASCov |
|----------|---------|---------|-----------|-------------|-------------|-------------|---------------|--------------|----------|----------|--------|
| 1.00 | 1.00 | 1.00 | 1.00 | 1.00 | 0.42 | 1.00 | 0.67 | 0.88 | 0.50 | 0.43 | 0.83 |

- 覆盖率解读：MASCov=0.83，AgentCov=1.00，ToolCov=1.00，EdgeCov=1.00，ReqVerifiedCov=1.00，EffectiveWorkflowRate=1.00，TraceCompleteness=0.67。该解读由确定性覆盖率产物汇总得到。
- 故障概要：检测到故障条目 8 个，其中确认主根因 5 个，派生症状 1 个，疑似误报 3 个，根因组 7 个。
- Pattern 选择：mode=agent_verified；selected=`tool_api_contract`, `tool_error_contract`, `scalable_budget`, `autogen_wiring`；promoted=None；diagnostic=`message_handoff_integrity`；rejected=13
- 测试框架/软预算类排除：13

#### 真实故障 / 确认主根因
| Fault ID | Case | Layer | Type | Severity | Confidence | EvidenceStrength | RootCauseConfidence | Summary | Suggested Fix |
|----------|------|-------|------|----------|------------|------------------|---------------------|---------|---------------|
| `SYSTEM2_RESEARCH_AGENTS_FAULT_001` | `system2_research_agents_BUDGET_001` | autogen_framework | Speaker Selection Error | high | 0.86 | 0.71 | trace_only | The GroupChat speaker selection path appears to loop on empty, invalid, or noisy speaker responses. | Enforce max_turns limit strictly in speaker selection by checking turn count before selecting the next speaker and terminating immediately when the limit is reached, rather than... |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_004` | `system2_research_agents_STATIC_human_input_requested` | autogen_framework | Human Input Mode Error | high | 0.9 | 0.76 | code_evidence | AutoGen user proxy is configured to always request human input in an automated workflow. | Set human_input_mode='NEVER' or provide a deterministic non-blocking input adapter for automated runs. |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_005` | `system2_research_agents_STATIC_view_parameter_ignored` | application | Tool API Semantics Error | high | 0.88 | 0.72 | code_evidence | External table/API tool constructs requests without preserving documented view/filter parameters. | Parse the semantic fields (base, table, view, query, filterByFormula, etc.) from the tool input and explicitly include them as query parameters in the API request URL. Ensure th... |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_006` | `system2_research_agents_STATIC_pagination_not_followed` | application | Tool API Pagination Missing | high | 0.88 | 0.72 | code_evidence | External table/API tool does not follow paginated responses. | Refactor the Airtable tool functions to loop while an 'offset' is present in the response, accumulate all records from successive pages, and return the complete merged list. Ens... |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_008` | `system2_research_agents_STATIC_scalable_budget_exceeded` | autogen_framework | Scalable Turn Budget Error | medium | 0.82 | 0.68 | code_evidence | GroupChat uses a fixed max_round for work that scales with records/items. | Scale max_round with record count or move repeated per-record work into deterministic batched tools. For example: calculate max_round as BASE_ROUNDS + (num_records * ROUNDS_PER_... |

#### 疑似误报
| Fault ID | Case | Layer | Type | Severity | Confidence | EvidenceStrength | RootCauseConfidence | Summary | Suggested Fix |
|----------|------|-------|------|----------|------------|------------------|---------------------|---------|---------------|
| `SYSTEM2_RESEARCH_AGENTS_FAULT_002` | `system2_research_agents_COV_003` | autogen_framework | Non-Termination | high | 0.82 | 0.6 | trace_only | The process exceeded the configured timeout. | 1) Define an explicit termination message e.g., a custom function `is_termination_msg` that checks for 'APPROVED' or 'TASK_COMPLETE' in chat content. 2) In the group chat config... |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_003` | `system2_research_agents_REQ_001` | application | Missing Tool Call | medium | 0.7 | 0.28 | trace_only | Expected tool was not called: google_search | Review tool registration and agent configuration to ensure 'google_search' is available to the researcher agent. Check if the tool was called within the 23-turn conversation but... |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_007` | `system2_research_agents_STATIC_tool_unstructured_error` | application | Tool Error Contract Missing | medium | 0.72 | 0.5 | code_evidence | HTTP tool can return raw text or None instead of a structured success/error envelope. | Check status codes and return typed success/error payloads with status, message, and retryability. |

#### 派生症状
| Fault ID | Case | Layer | Type | Severity | Confidence | EvidenceStrength | RootCauseConfidence | Summary | Suggested Fix |
|----------|------|-------|------|----------|------------|------------------|---------------------|---------|---------------|
| `SYSTEM2_RESEARCH_AGENTS_FAULT_003` | `system2_research_agents_REQ_001` | application | Missing Tool Call | medium | 0.7 | 0.28 | trace_only | Expected tool was not called: google_search | Review tool registration and agent configuration to ensure 'google_search' is available to the researcher agent. Check if the tool was called within the 23-turn conversation but... |

#### 非目标排除
| Case | Code | Layer | Type | Message | Suggested Fix |
|------|------|-------|------|---------|---------------|
| `system2_research_agents_BUDGET_001` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_COV_002` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_COV_004` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_COV_005` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_FUZZ_001` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_FUZZ_002` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_FUZZ_003` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_PROP_001` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_REQ_001` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_REQ_005` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_TOOLAPI_001` | `CONTRACT_TEST_NOT_EXERCISED` | test_harness | Contract Fixture Not Exercised | HTTP/API contract was configured, but no target HTTP request was observed; expected fixture values are not target evidence. | Improve the case adapter or instrumentation so the target actually performs the expected HTTP/tool action before judging the contract. |
| `system2_research_agents_TOOLCONTRACT_004` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_TOOLERR_001` | `CONTRACT_TEST_NOT_EXERCISED` | test_harness | Contract Fixture Not Exercised | HTTP/API contract was configured, but no target HTTP request was observed; expected fixture values are not target evidence. | Improve the case adapter or instrumentation so the target actually performs the expected HTTP/tool action before judging the contract. |

## 判定口径
- 真实故障：未被误报审计标记为 suspected false positive，且属于 application 或 autogen_framework 层的主根因。
- 派生症状：由同一主根因级联产生的附带失败，不重复计为新的主根因。
- 疑似误报：确定性 evidence gate 未达到确认阈值的目标层发现；agent 审计只作为辅助风险提示。
- 非目标排除：模型/API provider 超时、鉴权、测试框架软预算、未观测到有效工作流等，不计入目标系统真实故障。
