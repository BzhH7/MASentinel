# MASentinel 故障报告：system2_research_agents

> 本报告由 MASentinel 自动化测试系统基于该被测系统的 `coverage.json`、`faults.json`、`false_positive_audit.json`、`non_target_issues.json` 和运行 trace 自动生成。

## 测试结果概览

| Cases | 进程通过 | 进程失败 | Oracle 通过 | Oracle 失败 | 确认主根因 | 派生症状 | 疑似误报 | 非目标排除 |
|-------|----------|----------|-------------|-------------|------------|----------|----------|------------|
| 16 | 9 | 7 | 8 | 8 | 2 | 0 | 2 | 4 |

## 覆盖率

| AgentCov | ToolCov | EdgeCov | ReqCov | StateCov | FaultCov | MASCov |
|----------|---------|---------|--------|----------|----------|--------|
| 1.00 | 1.00 | 1.00 | 1.00 | 0.62 | 0.33 | 0.83 |

- 覆盖率解读：MASCov=0.83，AgentCov=1.00，ToolCov=1.00，EdgeCov=1.00。该解读由确定性覆盖率产物汇总得到。
- 故障概要：检测到故障条目 4 个，其中确认主根因 2 个，派生症状 0 个，疑似误报 2 个，根因组 4 个。
- 测试框架/软预算类排除：4

#### 真实故障 / 确认主根因
| Fault ID | Case | Layer | Type | Severity | Confidence | Summary | Suggested Fix |
|----------|------|-------|------|----------|------------|---------|---------------|
| `SYSTEM2_RESEARCH_AGENTS_FAULT_002` | `system2_research_agents_COV_001` | autogen_framework |  böjnings Termination | high | 0.9 | The process exceeded the configured timeout. | 1. Add a max retry limit in the speaker selection logic (e.g., after 3 empty responses, default to 'user_proxy' or 'director'). 2. Implement a fallback speaker selection rule wh... |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_004` | `system2_research_agents_TOOLFUZZ_001` | application | Encoding/Decoding Error | high | 0.9 | The process ended with an unhandled runtime error. | In the checking_agent's speaker validation logic (or the speaker_selection_agent's response processing), add string normalization: strip whitespaceantu and remove known prefixes... |

#### 疑似误报
| Fault ID | Case | Layer | Type | Severity | Confidence | Summary | Suggested Fix |
|----------|------|-------|------|----------|------------|---------|---------------|
| `SYSTEM2_RESEARCH_AGENTS_FAULT_001` | `system2_research_agents_COV_001` | autogen_framework | Human Input Mode Error | high | 0.9 | The target system requested human input during an automated no-human run. | In the test harness or agent configuration, explicitly set human_input_mode='NEVER' for all agents involved in automated execution. Ensure no blocking input() calls are present... |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_003` | `system2_research_agents_COV_002` | autogen_framework | Message Routing Error | medium | 0.2 | Expected message edge was not observed: director->research_manager | This is a likely false positive for a software fault. To improve coverage, consider adding a more specific prompt that forces director to delegate to research_manager, or adjust... |

#### 派生症状
- 无

#### 非目标排除
| Case | Code | Layer | Type | Message | Suggested Fix |
|------|------|-------|------|---------|---------------|
| `system2_research_agents_FUZZ_001` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_PROP_002` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_REQ_001` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_REQ_005` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |

## 判定口径
- 真实故障：未被误报审计标记为 suspected false positive，且属于 application 或 autogen_framework 层的主根因。
- 派生症状：由同一主根因级联产生的附带失败，不重复计为新的主根因。
- 疑似误报：FalsePositiveAuditorAgent 或确定性规则标记为 suspected_false_positive 的目标层发现。
- 非目标排除：模型/API provider 超时、鉴权、测试框架软预算、未观测到有效工作流等，不计入目标系统真实故障。
