# MASentinel 故障报告：system2_research_agents

> 本报告由 MASentinel 自动化测试系统基于该被测系统的 `coverage.json`、`faults.json`、`false_positive_audit.json`、`non_target_issues.json` 和运行 trace 自动生成。

## 测试结果概览

| Cases | 进程通过 | 进程失败 | Oracle 通过 | Oracle 失败 | 确认主根因 | 派生症状 | 疑似误报 | 非目标排除 |
|-------|----------|----------|-------------|-------------|------------|----------|----------|------------|
| 32 | 25 | 7 | 3 | 29 | 3 | 1 | 1 | 10 |

## 覆盖率

| AgentCov | ToolCov | EdgeCov | ReqIntent | ReqVerified | ContractCov | EffWorkflow | TraceComplete | EvidenceRate | StateCov | FaultCov | MASCov |
|----------|---------|---------|-----------|-------------|-------------|-------------|---------------|--------------|----------|----------|--------|
| 1.00 | 1.00 | 1.00 | 1.00 | 1.00 | 0.33 | 1.00 | 0.67 | 0.75 | 0.54 | 0.43 | 0.84 |

- 覆盖率解读：MASCov=0.84，AgentCov=1.00，ToolCov=1.00，EdgeCov=1.00，ReqVerifiedCov=1.00，EffectiveWorkflowRate=1.00，TraceCompleteness=0.67。该解读由确定性覆盖率产物汇总得到。
- 故障概要：检测到故障条目 4 个，其中确认主根因 3 个，派生症状 1 个，疑似误报 1 个，根因组 3 个。
- Pattern 选择：mode=agent_verified；selected=`tool_api_contract`, `tool_error_contract`, `autogen_wiring`；diagnostic=`message_handoff_integrity`；rejected=15
- 测试框架/软预算类排除：10

#### 真实故障 / 确认主根因
| Fault ID | Case | Layer | Type | Severity | Confidence | EvidenceStrength | RootCauseConfidence | Summary | Suggested Fix |
|----------|------|-------|------|----------|------------|------------------|---------------------|---------|---------------|
| `SYSTEM2_RESEARCH_AGENTS_FAULT_001` | `system2_research_agents_COV_001` | autogen_framework | Speaker Selection Error | high | 0.86 | 0.91 | trace_only | The GroupChat speaker selection path appears to loop on empty, invalid, or noisy speaker responses. | Harden GroupChat speaker selection: normalize speaker names, handle empty responses, and cap repeated retries. |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_003` | `system2_research_agents_COV_002` | autogen_framework | Non-Termination | high | 0.82 | 0.8 | trace_only | The process exceeded the configured timeout. | Add a termination message function that checks for keywords like 'TERMINATE' or 'exit'. Configure max_turns=12 in groupchat/agent settings and set human_input_mode='NEVER' for a... |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_004` | `system2_research_agents_REQ_003` | application | Missing Error Handling | high | 0.85 | 0.66 | trace_only | The process ended with an unhandled runtime error. | Modify the code generation logic or the Python execution environment to ensure all required tools (web_scraping, google_search, get_airtable_records, update_single_airtable_reco... |

#### 疑似误报
| Fault ID | Case | Layer | Type | Severity | Confidence | EvidenceStrength | RootCauseConfidence | Summary | Suggested Fix |
|----------|------|-------|------|----------|------------|------------------|---------------------|---------|---------------|
| `SYSTEM2_RESEARCH_AGENTS_FAULT_002` | `system2_research_agents_COV_001` | application | Missing Tool Call | medium | 0.7 | 0.28 | trace_only | Expected tool was not called: update_single_airtable_record | Verify tool registration and prompting; ensure the target agent has access to the tool. |

#### 派生症状
| Fault ID | Case | Layer | Type | Severity | Confidence | EvidenceStrength | RootCauseConfidence | Summary | Suggested Fix |
|----------|------|-------|------|----------|------------|------------------|---------------------|---------|---------------|
| `SYSTEM2_RESEARCH_AGENTS_FAULT_002` | `system2_research_agents_COV_001` | application | Missing Tool Call | medium | 0.7 | 0.28 | trace_only | Expected tool was not called: update_single_airtable_record | Verify tool registration and prompting; ensure the target agent has access to the tool. |

#### 非目标排除
| Case | Code | Layer | Type | Message | Suggested Fix |
|------|------|-------|------|---------|---------------|
| `system2_research_agents_COV_004` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_COV_005` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_COV_008` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_COV_009` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_FUZZ_001` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_PROP_001` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_REQ_004` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_TOOLAPI_001` | `CONTRACT_TEST_NOT_EXERCISED` | test_harness | Contract Fixture Not Exercised | HTTP/API contract was configured, but no target HTTP request was observed; expected fixture values are not target evidence. | Improve the case adapter or instrumentation so the target actually performs the expected HTTP/tool action before judging the contract. |
| `system2_research_agents_TOOLCONTRACT_004` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_TOOLERR_001` | `CONTRACT_TEST_NOT_EXERCISED` | test_harness | Contract Fixture Not Exercised | HTTP/API contract was configured, but no target HTTP request was observed; expected fixture values are not target evidence. | Improve the case adapter or instrumentation so the target actually performs the expected HTTP/tool action before judging the contract. |

## 判定口径
- 真实故障：未被误报审计标记为 suspected false positive，且属于 application 或 autogen_framework 层的主根因。
- 派生症状：由同一主根因级联产生的附带失败，不重复计为新的主根因。
- 疑似误报：确定性 evidence gate 未达到确认阈值的目标层发现；agent 审计只作为辅助风险提示。
- 非目标排除：模型/API provider 超时、鉴权、测试框架软预算、未观测到有效工作流等，不计入目标系统真实故障。
