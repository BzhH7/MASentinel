# 服务器运行结果分析

分析对象：根目录 `outputs/` 下的三套服务器运行结果。

## 1. 总览结论

这次服务器运行的证据链是完整的：

- 三个系统都有 `run_manifest.json`，且 `human_intervention_allowed=false`；
- 三个系统都有 `testcases.generated.json`、`testcases.validated.json`、`testcases.frozen.sha256`；
- 三个系统都有 `runs/traces/*.json`、`oracle_results.json`、`faults.json`、`false_positive_audit.json`、`coverage.json`；
- trace metadata 中记录了被测系统模型为 `ds-v4-flash`，说明目标系统运行侧确实接收到了 Flash 配置；
- `model_usage.json` 中记录了 MASentinel 测试智能体调用 `ds-v4-pro`，说明测试方案侧使用了 Pro。

但运行结果不能只看 process pass/fail。当前结果里：

| System | Process Runs | Process Passed | Oracle Passed | Faults | Suspected FP | MASCov | 主要问题 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| system1_iterative_coding | 16 | 0 | 0 | 6 | 0 | 0.3750 | AutoGen 版本配置不兼容，启动前失败 |
| system2_research_agents | 16 | 0 | 0 | 5 | 2 | 0.3067 | 运行环境缺依赖，启动前失败 |
| system3_financial_analysis | 16 | 16 | 4 | 2 | 2 | 0.3467 | 数据源限流，agent 协作未真正展开 |

结论：这次运行可以作为“框架自动化闭环证据”，但还不适合作为最终效果数据。最终报告前建议修复 system1/system2 的公共阻断，并给 system3 加数据源失败 oracle。

## 2. 模型使用情况

### 被测系统模型

三套 trace 的 metadata 均记录：

```text
target_model = ds-v4-flash
target_api_key_env = INF_API_KEY_FLASH
```

说明 MASentinel runner 已将被测系统模型设置为 DeepSeek V4 flash。

### 测试方案模型

`model_usage.json` 记录：

| System | Total Calls | Successful | Fallback | Failed |
| --- | ---: | ---: | ---: | ---: |
| system1_iterative_coding | 18 | 13 | 5 | 5 |
| system2_research_agents | 16 | 7 | 9 | 9 |
| system3_financial_analysis | 10 | 7 | 3 | 3 |

主要 fallback 原因：

- `Model call failed: The read operation timed out`
- `Agent returned non-JSON or empty JSON`

这说明 DeepSeek V4 pro 已参与测试智能体流程，但部分调用因为超时或非 JSON 输出走了 deterministic fallback。fallback 不影响整体评测闭环，但会影响报告叙述质量和诊断细节。

## 3. system1_iterative_coding 分析

### 结果摘要

- 16 条 process run 全失败；
- 16 条 oracle result 全失败；
- 主要 failure codes：
  - `RUNTIME_EXCEPTION`: 16
  - `TOOL_SCHEMA_MISMATCH`: 16
  - `MISSING_AGENT`: 16
  - `MISSING_TOOL_CALL`: 4
  - `MISSING_MESSAGE_EDGE`: 3
  - `METAMORPHIC_RELATION_VIOLATION`: 1

### 关键证据

代表性 stderr：

```text
TypeError: LLMConfig.__init__() got an unexpected keyword argument 'request_timeout'
```

这说明 system1 没有真正进入多智能体协作，而是在 AutoGen 初始化 LLMConfig 时失败。它不是测例设计导致的业务失败，而是目标系统代码与服务器 AutoGen 版本不兼容。

### 判断

当前 0 pass 是合理的，因为所有 case 都被同一个启动错误阻断。这里不应该解释为“MASentinel 测例全失败”，而应解释为：

```text
MASentinel 自动发现 system1 存在 AutoGen 配置兼容性故障。
```

### 建议

1. 在 `AutoGen_IterativeCoding-main/main.py` 和 `IterativeTools.py` 中移除或替换 `request_timeout`；
2. 保留 `AUTOGEN_USE_DOCKER=0`，避免 Docker 未运行导致公共阻断；
3. system1 本身还有交互式 `input()` 流程，修复 LLMConfig 后可能继续触发 `HUMAN_INPUT_REQUESTED`，这是另一个真实自动化兼容性问题。

## 4. system2_research_agents 分析

### 结果摘要

- 16 条 process run 全失败；
- 16 条 oracle result 全失败；
- 主要 failure codes：
  - `RUNTIME_EXCEPTION`: 16
  - `MISSING_AGENT`: 27
  - `MISSING_MESSAGE_EDGE`: 9
  - `MISSING_TOOL_CALL`: 6
  - `METAMORPHIC_RELATION_VIOLATION`: 1

### 关键证据

代表性 stderr：

```text
ModuleNotFoundError: No module named 'langchain.text_splitter'
```

这说明 system2 在 import 阶段就失败，没有进入 AutoGen agent 协作，也没有调用工具。

### 判断

当前结果主要是运行环境/依赖问题，不是业务逻辑测试结果。MASentinel 正确地把它归类成环境依赖类 runtime exception。

### 建议

1. 安装/匹配 LangChain 依赖，或兼容新版导入路径 `langchain_text_splitters`；
2. system2 代码中 `UserProxyAgent(human_input_mode="ALWAYS")` 会在自动化评测中继续产生风险，修复依赖后可能触发 `HUMAN_INPUT_REQUESTED`；
3. 该系统还依赖 Serper、Browserless、Airtable 等外部服务 key，需要区分“外部服务缺失/失败”和“AutoGen 编排缺陷”。

## 5. system3_financial_analysis 分析

### 结果摘要

- 16 条 process run 全部 exit code 0；
- oracle 层只有 4 条 pass，12 条 fail；
- dedup 后 faults 为 2，且均标记为 suspected false positive；
- process pass 与 oracle pass 不一致。

### 关键证据

代表性 stderr：

```text
数据收集失败 AAPL: Too Many Requests. Rate limited. Try after a while.
分析失败: 无法收集数据
```

这说明程序虽然以 exit code 0 结束，但实际分析没有成功完成。因为数据源被限流，agent 协作没有充分展开，导致 agent coverage 为 0。

### 判断

system3 的 16 个 process pass 不能理解为“系统全部通过”。更准确的结论是：

```text
目标程序没有崩溃，但业务任务因为数据源限流未完成；当前 oracle 对“分析失败但 exit code 0”的情况还不够严格。
```

### 建议

1. 在 oracle 中加入业务失败关键字检测，例如 `分析失败`、`无法收集数据`、`Too Many Requests`；
2. 给 system3 配置离线/mock 数据源，避免 yfinance 限流影响 AutoGen 流程测试；
3. 对 system3 的 missing-agent fault 保持谨慎，因为默认 trace 未 hook AutoGen 内部消息，当前两个 fault 已标记为 suspected false positive。

## 6. 覆盖率解释

三套系统的 MASCov 都偏低：

| System | AgentCov | ToolCov | EdgeCov | ReqCov | StateCov | FaultCov | MASCov |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| system1 | 0.25 | 0.00 | 0.00 | 1.00 | 0.5625 | 0.50 | 0.3750 |
| system2 | 0.00 | 0.00 | 0.00 | 1.00 | 0.50 | 0.4167 | 0.3067 |
| system3 | 0.00 | 1.00 | 0.00 | 0.50 | 0.375 | 0.1667 | 0.3467 |

偏低原因不是测例数量不足，而是目标系统没有充分进入 agent/tool/message 流程：

- system1：LLMConfig 初始化失败；
- system2：import 阶段依赖失败；
- system3：数据源限流，任务提前返回错误。

## 7. 对最终报告的写法建议

这次结果可以这样写：

> MASentinel 在三个系统上均完成了无人干预的自动化评测闭环，生成了测例、执行 trace、oracle 判定、覆盖率与故障报告。运行结果显示，system1 和 system2 存在公共启动阻断，分别对应 AutoGen 配置兼容性错误与依赖缺失；system3 进程未崩溃，但因数据源限流未完成业务分析，暴露出现有 oracle 对业务失败语义检测不足的问题。

不要写成：

```text
三个系统测试效果不好，很多 case 没过。
```

更准确的说法是：

```text
测试框架成功定位到目标系统运行环境、AutoGen 配置、外部数据源与自动化交互兼容性问题。
```

## 8. 下一轮优先级

建议按以下顺序修：

1. system1：修 `request_timeout` 与 Docker 配置，重新跑；
2. system2：修 LangChain 依赖/导入路径，重新跑；
3. system3：加入数据源 mock 或业务失败 oracle；
4. MASentinel：区分 process pass 与 oracle pass，在 summary 中同时显示；
5. MASentinel：对 `分析失败`、`无法收集数据`、`Too Many Requests` 等业务失败文本增加 oracle failure code。

