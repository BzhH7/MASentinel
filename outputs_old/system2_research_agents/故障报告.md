# MASentinel 故障报告：system2_research_agents

> 本报告由 MASentinel 自动化测试系统基于该被测系统的 `coverage.json`、`faults.json`、`false_positive_audit.json`、`non_target_issues.json` 和运行 trace 自动生成。

## 测试结果概览

| Cases | 进程通过 | 进程失败 | Oracle 通过 | Oracle 失败 | 确认主根因 | 派生症状 | 疑似误报 | 非目标排除 |
|-------|----------|----------|-------------|-------------|------------|----------|----------|------------|
| 32 | 21 | 11 | 2 | 30 | 4 | 1 | 1 | 7 |

## 覆盖率

| AgentCov | ToolCov | EdgeCov | ReqIntent | ReqVerified | ContractCov | EffWorkflow | TraceComplete | EvidenceRate | StateCov | FaultCov | MASCov |
|----------|---------|---------|-----------|-------------|-------------|-------------|---------------|--------------|----------|----------|--------|
| 1.00 | 1.00 | 1.00 | 1.00 | 1.00 | 0.64 | 0.97 | 0.67 | 0.67 | 0.58 | 0.52 | 0.86 |

- 覆盖率解读：MASCov 综合覆盖率为 0.8571。agent、tool 和消息边覆盖率均达 1.0，需求相关覆盖率 1.0，trace 完整度 0.6667，根因证据率 0.6667。覆盖表现整体良好，但状态覆盖率 0.5833、故障模式覆盖率 0.5238 和契约覆盖率 0.6364 相对较低，表明异常路径与状态组合仍有测试空间。
- 故障概要：共 32 个测试用例，21 个进程正常结束（11 个失败/超时），但 oracle 仅 2 个通过（30 个失败），显示大量 oracle 判定失效可能源于超时。检测总故障 6 个，其中已确认 5 个（主要根因 4 个，衍生症状 1 个），疑似误报 1 个。另有 7 个非目标问题（TURN_BUDGET_EXCEEDED 等）被排除。
- 测试框架/软预算类排除：7

#### 真实故障 / 确认主根因
| Fault ID | Case | Layer | Type | Severity | Confidence | EvidenceStrength | RootCauseConfidence | Summary | Suggested Fix |
|----------|------|-------|------|----------|------------|------------------|---------------------|---------|---------------|
| `SYSTEM2_RESEARCH_AGENTS_FAULT_001` | `system2_research_agents_ARTIFACT_001` | autogen_framework | Speaker Selection Error | high | 0.86 | 0.71 | trace_only | The GroupChat speaker selection path appears to loop on empty, invalid, or noisy speaker responses. | Harden GroupChat speaker selection logic: 1) Implement explicit termination detection when the conversation indicates task completion (e.g., check for 'Task complete' or final a... |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_002` | `system2_research_agents_ARTIFACT_001` | autogen_framework | Non-Termination | high | 0.82 | 0.6 | trace_only | The process exceeded the configured timeout. | Add a termination message check (e.g., if 'Task complete' appears, set is_termination_msg=true), enforce a strict max_turns limit, and/or configure human_input_mode='NEVER' with... |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_004` | `system2_research_agents_TOOLAPI_001` | application | Tool Schema Mismatch | high | 0.9 | 0.72 | code_evidence | External API request did not preserve documented semantic query parameters. | Modify get_airtable_records and update_single_airtable_record in app.py to: 1) parse and pass semantic fields such as view/base/table into the API request; 2) follow offset-base... |
| `SYSTEM2_RESEARCH_AGENTS_FAULT_005` | `system2_research_agents_TOOLAPI_001` | application | Tool API Pagination Missing | high | 0.9 | 0.72 | code_evidence | External API pagination stopped before all fixture pages were requested. | Modify the get_airtable_records function in app.py to: (1) Parse and preserve semantic URL parameters such as view/base/table from the request. (2) Implement a loop that follows... |

#### 疑似误报
| Fault ID | Case | Layer | Type | Severity | Confidence | EvidenceStrength | RootCauseConfidence | Summary | Suggested Fix |
|----------|------|-------|------|----------|------------|------------------|---------------------|---------|---------------|
| `SYSTEM2_RESEARCH_AGENTS_FAULT_006` | `system2_research_agents_TOOLERR_001` | application | Tool Error Contract Missing | medium | 0.88 | 0.3 | trace_only | HTTP failure status was not captured in the trace envelope. | Modify the web_scraping tool wrapper to check HTTP response status codes. On non-2xx responses, construct and return a structured error dictionary (e.g., {"error": True, "status... |

#### 派生症状
| Fault ID | Case | Layer | Type | Severity | Confidence | EvidenceStrength | RootCauseConfidence | Summary | Suggested Fix |
|----------|------|-------|------|----------|------------|------------------|---------------------|---------|---------------|
| `SYSTEM2_RESEARCH_AGENTS_FAULT_003` | `system2_research_agents_REQ_002` | application | Missing Tool Call | medium | 0.2 | 0.28 | trace_only | Expected tool was not called: get_airtable_records | 1) Enable detailed tool-call logging (e.g. log each AutoGen tool invoke/return). 2) Re-run with verbose LLM call traces to confirm whether the director agent attempted to call g... |

#### 非目标排除
| Case | Code | Layer | Type | Message | Suggested Fix |
|------|------|-------|------|---------|---------------|
| `system2_research_agents_COV_001` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_COV_003` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_COV_005` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_COV_006` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_PROP_001` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_REQ_004` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |
| `system2_research_agents_TOOLCONTRACT_004` | `TURN_BUDGET_EXCEEDED` | test_harness | Soft Turn Budget Exceeded | The run terminated, but exceeded the expected soft turn budget. | Treat max_turns as a soft efficiency budget, adjust the generated oracle budget, and do not count this as an application/framework defect by itself. |

## 判定口径
- 真实故障：未被误报审计标记为 suspected false positive，且属于 application 或 autogen_framework 层的主根因。
- 派生症状：由同一主根因级联产生的附带失败，不重复计为新的主根因。
- 疑似误报：FalsePositiveAuditorAgent 或确定性规则标记为 suspected_false_positive 的目标层发现。
- 非目标排除：模型/API provider 超时、鉴权、测试框架软预算、未观测到有效工作流等，不计入目标系统真实故障。
